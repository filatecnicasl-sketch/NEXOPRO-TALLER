
## 2026-07 (fork) — Editor de formatos de impresión (Fases 1 y 2)
- FASE 1: Portado un editor de formatos de impresión (arrastrar/redimensionar elementos en mm sobre A4/A5: texto, campo, área, casilla, imagen, línea, rect, tabla, firma) a NexoPro en JavaScript, en `src/formatos/*`. Modos Diseñar/Rellenar. Plantillas guardadas en MongoDB (colección `formatos_impresion`), CRUD backend en server.py (~línea 2953): GET/POST/PUT/DELETE `/api/formatos`. Ruta `/ajustes/formatos` (pantalla completa) + tarjeta en Ajustes. Plantilla de fábrica "Recepción de Vehículo" (resguardo de depósito). Verificado testing_agent iteration_19 (100%).
- FASE 2: Al imprimir la "Hoja de entrada" desde Órdenes de trabajo o Recepción rápida, ahora usa la plantilla del editor rellena con datos reales (mapOrdenToFormData → claves taller.*, cliente.*, vehiculo.*, resguardo.numero). `src/formatos/printTemplate.jsx`: printTemplateWithData() renderiza PrintSheet con createRoot (React 19) y llama window.print(); getPlantillaHojaEntrada() usa ajustes.formato_hoja_entrada o la plantilla "Recepción de Vehículo". Selector en Ajustes (data-testid=select-formato-hoja-entrada) para elegir la plantilla; backend ajustes campo `formato_hoja_entrada`. Fallback a la impresión HTML antigua si no hay plantilla. Verificado por smoke test (window.print llamado, datos reales renderizados).
- Cambio de nomenclatura: "Peritaje/Peritajes" → "Valoración/Valoraciones" en toda la UI (código interno intacto).
- Módulo de inicio configurable (Ajustes: modulo_inicio panel/taller) con redirección en App.js (Inicio, 1 vez por sesión).
- Eliminado badge "Made with Emergent" de public/index.html. Añadido lang="es" + meta notranslate (soluciona crash React removeChild por traducción automática de Chrome).
- Impresión unificada en la propia página (imprimirHtmlString: iframe→ inyección oculta + window.print), sin ventanas nuevas ni PDF (evita ERR_BLOCKED_BY_CLIENT). Hoja de entrada backend cambiada a A4 vertical.
